/*
Question:
Write a program to calculate overtime pay of 10 employees.
Overtime is paid at the rate of Rs. 12.00 per hour for every
hour worked above 40 hours. Assume that employees do not
work for fractional part of an hour
*/


#include <stdio.h>

/*
int main()
{
	int overtimePay[10] =  {5,10,14,5,6,7,3,2,0,10};
	
	int i, total, ratePerPerson;
	for(i = 0; i<10; i++)
	{	
		ratePerPerson = overtimePay[i]*12;
		printf("\n\n\nOvertime worked hours of person of index %d is %d",i, overtimePay[i]);
		printf("\nOvertime of person at index %d is %d",i , ratePerPerson);
		total += ratePerPerson;
	}
	
	printf("\n\n\nTotal overtime paid: %d", total);

	return 0;
}
*/
