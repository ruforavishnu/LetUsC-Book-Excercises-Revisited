#include <stdio.h>
#include <math.h>

double power(double a, double b);

/*
int main()
{

	double a,b,p;
	
	printf("\nEnter values for a and b:\n");
	scanf("%lf %lf", &a,&b);
	
	printf("\n\nThe input values are %f and %f", a,b);
	
	
	p = power(a,b);
	printf("\nThe power is %lf", p);

	return 0;
}

double power(double a, double b)
{
	double p = pow(a,b);
	return p;
}
*/
