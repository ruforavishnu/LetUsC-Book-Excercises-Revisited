#include <stdio.h>

#include<math.h>

/*
int main()
{
	int q,p,n;
	float r,a;
	printf("\n2 raised to 4 is %d\n", pow(2,4));
	return 0;
	
	printf("Enter principle, rate of interest and number of years:\n");
	scanf("%d %f %d",&p,&r,&n);
	
	printf("Enter number of times to be compounded in a year:\n");
	scanf("%d", &q);
	
	a = p*pow((1 + (r/n) ), n );
	
	
	printf("Final compounded amount is: %f", a);
	
	
	
	
	return 0;
}
*/
