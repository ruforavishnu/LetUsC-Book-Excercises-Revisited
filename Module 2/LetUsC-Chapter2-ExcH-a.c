#include <stdio.h>

/*
int main() 
{
	int tag = 0, code = 1;
	if(tag == 0)
		(code > 1? printf("\nHello") ? printf("\nHi"));
	else
		printf("\nHello Hi!");

	return 0;
}

*/
//	[Error] expected ':' before ')' token
