/*
Two numbers are entered through the keyboard. Write a
program to find the value of one number raised to the power
of another
*/

#include <stdio.h>

/*
int main()
{
	int a,b,pow,i;
	
	printf("\nEnter a and b:\n");
	scanf("%d %d",&a,&b);
	
	pow = 1;
	for(i = 0; i<b; i++)
	{
		pow = pow*a;
	}
	
	printf("The value of %d to the power %d is %d",a,b,pow);
	
	return 0;
}
*/
