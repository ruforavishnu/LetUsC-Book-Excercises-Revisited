#include <stdio.h>

/*
int main()
{
	
	int i = 1;
	int fact = 1;
	int val = 0;
	float sum = 0;
	float m = 0.0;
	
	for(i = 1; i <= 7; i++)
	{
		fact = 1;
		val = i;
			
		if(i == 1)
			fact = 1;
		else if(i == 2)
			fact = 2;
		else
		{
			while(val > 1)	
			{
				fact = fact * val;	
				--val;
			}	
		}
//		printf("\ni is %d and Sum is being added by %d",i,fact);
//		printf("\ni is %d. i/fact-i is  %d/%d \n\n",i,i,fact);
		m = (float)i/fact;
		printf("\ni is %d\t", i);
		printf("fact-%d is %d\t",i, fact);
		printf("%d/fact-%d is %f",i,i,m);
		
		//printf("\n i is %d.\tfact-%d is %d.\t %d/fact-%d is %f\tval=%d,fact=%d",i, i,fact,i,i, m,i, fact );
		
		sum = sum + m;
		
	}
	
	printf("\n\nValue : %f", sum);
	
	return 0;
}
*/

/*
Calculations

fact 1 = 1;
fact 2 = 1 x 2 = 2
fact 3 = 1 x 2 x 3;



*/
