/*Question: Any year is input through the keyboard. Write a program to
determine whether the year is a leap year or not.
(Hint: Use the % (modulus) operator)
*/

#include <stdio.h>
/*
int main()
{
	int year = 0;
	
	printf("Enter any year between 0AD and 10,000AD\n");
	scanf("%d", &year);
	
	if(year%4==0)
		printf("The year %d is a leap year", year);
	else
		printf("The year %d is not a leap year", year);
	
	return 0;
}
*/
