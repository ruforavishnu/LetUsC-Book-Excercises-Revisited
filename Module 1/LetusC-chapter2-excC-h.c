/*Find the absolute value of a number entered through the
keyboard
*/

#include <stdio.h>
/*
int main()
{
	
	int number = 0;
	int originalNumber;
	
	printf("\nEnter a number\n");
	scanf("%d", &number);
	
	originalNumber = number;
	
	if(number < 0)
		number *= -1;
	
	printf("The absolute value of the number %d is %d", originalNumber, number);
	
	return 0;
}
*/
