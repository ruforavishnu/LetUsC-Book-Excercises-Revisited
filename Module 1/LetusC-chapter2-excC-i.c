/*Given the length and breadth of a rectangle, write a program to
find whether the area of the rectangle is greater than its
perimeter. For example, the area of the rectangle with length = 5
and breadth = 4 is greater than its perimete
*/

#include <stdio.h>
/*
int main()
{
	int length, breadth;
	
	printf("\nEnter the length and breadth of the rectangle\n");
	scanf("%d %d", &length, &breadth);
	
	int area = length * breadth;
	int perimeter = 2*(length+breadth);
	
	if(area > perimeter)
		printf("The area of the rectangle is greater than the perimeter. Area being %d and Perimeter being %d", area, perimeter);
		
	else
			printf("The area of the rectangle is lesser than the perimeter. Area being %d and Perimeter being %d", area, perimeter);
	
	return 0;
}
*/
