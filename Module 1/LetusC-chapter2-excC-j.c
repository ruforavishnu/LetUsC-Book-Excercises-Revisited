/*Given three points (x1, y1), (x2, y2) and (x3, y3), write a
program to check if all the three points fall on one straight line.
*/
#include <stdio.h>
#include <math.h>
/*
int main()
{
	float x1,y1, x2,y2, x3,y3;
	
	printf("\nEnter values of points x1,y1\n");
	scanf("%f %f", &x1,&y1);

	printf("\nEnter values of points x2,y2\n");
	scanf("%f %f", &x2,&y2);
	
	printf("\nEnter values of points x3,y3\n");
	scanf("%f %f", &x3,&y3);
	
	printf("The values of x1,x2,x3 and y1,y2,y3 is %f %f %f %f %f %f", x1,x2,x3, y1,y2,y3);
	
	float slope1 , slope2;
	
	slope1 = (y2-y1)/(x2-x1);
	slope2 = (y3-y2)/(x3-x2);
	
	printf("\ny2-y1 is %f", (y2-y1));
	printf("\nx2-x1 is %f", (x2-x1));	
	printf("\nSlope1 is %f and slope2 is %f", slope1, slope2);
	
	float abs = slope2-slope1;
	if(abs < 0)
		abs *= -1;
		
	if(abs < 0.001)
		printf("\nAll points are collinear");
	else
		printf("\nAll points are NOT collinear");
		
		
	printf("\n(slope2-slope1) is %f", (slope2-slope1));
	printf("\nabs(slope2-slope1) is %f", abs);
	
	
	
	return 0;
}
*/
