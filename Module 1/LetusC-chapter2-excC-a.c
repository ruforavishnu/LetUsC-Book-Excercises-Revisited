/*Question: If cost price and selling price of an item is input through the
keyboard, write a program to determine whether the seller has
made profit or incurred loss. Also determine how much profit
he made or loss he incurred.
*/

#include <stdio.h>
/*
int main()
{
	int costPrice = 0;
	int sellingPrice = 0;
	
	printf("Enter cost price: ");
	scanf("%d",&costPrice);
	printf("Enter selling price: ", &sellingPrice);
	scanf("%d",&sellingPrice);
	
	
	
	int margin = sellingPrice - costPrice;
	
	if(margin > 0)
		printf("The seller has made a profit of %d", margin);
	else if(margin < 0)
		printf("The seller has made a loss of %d", margin);
	else
		printf("The seller has breaken even. No profit or loss");
	
	
	return 0;
}
*/
