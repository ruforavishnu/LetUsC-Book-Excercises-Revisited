/*Given a point (x, y), write a program to find out if it lies on the
x-axis, y-axis or at the origin, viz. (0, 0)
*/

#include<stdio.h>

/*
int main()
{
	float x,y;
	printf("\nEnter a value for x,y\n");
	scanf("%f %f", &x,&y);

	if(x==0.0 && y==0.0)	
		printf("The point lies on the origin");
	else if(x==0.0)
		printf("The point lies on the y-axis");
	else if(y==0.0)
		printf("The point lies on the x-axis");
	else
		printf("The point does not lie on the x-axis or the y-axis");

		
	return 0;
}
*/
